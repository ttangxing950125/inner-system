<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deloitte.additional.recording.mapper.SysGroupMapper">
   <select id="getGroupCollocterIds" resultType="Integer">
       select DISTINCT u.id from sys_group g INNER JOIN sys_group_user gu ON g.id = gu.group_id
         INNER JOIN sys_user u ON u.id = gu.user_id
         INNER JOIN sys_user_role ur ON ur.user_id = u.id
         INNER JOIN sys_role r ON r.id = ur.role_id
       where r.`name` = '补录人员'
   </select>

    <select id="getGroupIds" resultType="long">
        SELECT
            gu.group_id
        FROM
            sys_group_user gu
                INNER JOIN sys_user_role ur ON ur.user_id = gu.user_id
                INNER JOIN sys_role r ON r.id = ur.role_id
        WHERE
            ur.user_id = #{currentUserId}
          AND r.`name` = #{name}
        GROUP BY
            gu.group_id
    </select>

    <select id="getUserGroupIds" resultType="long">
        select gu.user_id from sys_group_user gu INNER JOIN sys_group g ON gu.group_id = g.id
        <where>
             1=1
            <if test="groupIds!=null and groupIds.size()>0">
               and gu.group_id in
                <foreach collection="groupIds" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
        </where>
       GROUP BY gu.id
    </select>
</mapper>